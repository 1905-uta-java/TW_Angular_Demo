import { ConcatSpacedPipePipe } from './concat-spaced-pipe.pipe';

describe('ConcatSpacedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatSpacedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
